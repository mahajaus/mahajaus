pandas>=2.0
numpy>=1.24
pyarrow>=12.0        # only for reading/writing parquet (footprint --out-dir, study/backtest)
websockets>=11.0     # only for live streaming (live.py --live); not needed for replay/backtest
